registry=https://registry.npmmirror.com

# electron-builder 兼容性：pnpm 默认的符号链接 node_modules 结构
# 无法被 electron-builder 正确打包进 ASAR。
# hoisted 模式让 pnpm 生成和 npm 一样的扁平化 node_modules。
node-linker=hoisted
shamefully-hoist=true
