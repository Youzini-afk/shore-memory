[
  {
    "name": "add_reminder",
    "description": "Add a scheduled reminder.",
    "parameters": {
      "type": "object",
      "properties": {
        "time": { "type": "string", "description": "Natural language time string" },
        "content": { "type": "string", "description": "Reminder content" },
        "repeat_rule": {
          "type": "string",
          "enum": ["daily", "weekly"],
          "description": "Optional repetition rule"
        }
      },
      "required": ["time", "content"]
    }
  },
  {
    "name": "list_reminders",
    "description": "List all pending reminders.",
    "parameters": { "type": "object", "properties": {} }
  },
  {
    "name": "delete_reminder",
    "description": "Delete a reminder by ID.",
    "parameters": {
      "type": "object",
      "properties": { "id": { "type": "string" } },
      "required": ["id"]
    }
  }
]
