# MOD 目录

这是存放 PeroCore MOD 的目录。

详细的 MOD 开发指南请参阅 [MOD 开发指南](../../wiki/ecosystem/mod.md)。

## 快速开始

1. 在此目录下创建一个新文件夹，例如 `my_first_mod`。
2. 在该文件夹中创建 `mod_init.py` 文件。
3. 在 `mod_init.py` 中编写 `init()` 函数。

```python
def init():
    print("Hello from My First Mod!")
```
